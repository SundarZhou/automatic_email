<% provide(:title, "未结清刷卡记录") %>
<h1 class="page-header">未结清刷卡记录</h1>
<% if @records.present? %>
  <table class="table table-hover table-responsive">
    <thead>
      <tr>
        <th>银行</th>
        <th>卡号</th>
        <th>币种</th>
        <th>刷卡金额</th>
        <th>刷卡日期</th>
        <th>收款记录</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
      <% @records.each do |c| %>
        <tr>
          <% card = c.credit_card %>
          <td><%= card.bank_name %></td>
          <td><%= card.number %></td>
          <td><%= c.currency_name %></td>
          <td><%= c.amount %></td>
          <td><%= c.credit_date %></td>
          <td><%= c.repayment_record&.message %></td>
          <td><%= link_to'修改' , edit_credit_card_record_path(c), class: 'btn btn-info'%></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>